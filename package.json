{
  "name": "dalle-generate-and-download",
  "version": "1.0.0",
  "description": "Generate, download, and save images from Dall-E in a simple way",
  "main": "build/index.js",
  "scripts": {
    "clean": "rm -rf build",
    "build": "npm run clean && mkdir build && cp src/**.* build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "build/**/*"
  ],
  "keywords": [],
  "author": "igorpronin",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.2.2",
    "moment": "^2.29.4",
    "openai": "^3.1.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/igorpronin/dall-e-generate-and-download-img.git"
  }
}
